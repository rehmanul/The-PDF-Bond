
[build]
  base = "/"
  publish = "static"
  command = "pip install -r requirements.txt"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
